yolo11m-seg.pt filter=lfs diff=lfs merge=lfs -text
models/*.pth filter=lfs diff=lfs merge=lfs -text

# Exclude exiftool from language statistics (files still uploaded and usable)
exiftool_bundle/* linguist-vendored
exiftool linguist-vendored
